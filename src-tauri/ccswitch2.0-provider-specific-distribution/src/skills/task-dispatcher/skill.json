{
  "name": "task-dispatcher",
  "version": "1.0.0",
  "description": "将任务分派给不同的 AI 模型执行，如 Codex、Gemini 等",
  "author": "Claude Code Assistant",
  "commands": [
    {
      "name": "dispatch-task",
      "description": "将任务分派给指定的 AI 模型执行",
      "parameters": [
        {
          "name": "target",
          "type": "string",
          "description": "目标 AI 模型 (codex, gemini, claude, opencode)",
          "required": true
        },
        {
          "name": "task",
          "type": "string",
          "description": "要执行的任务",
          "required": true
        },
        {
          "name": "timeout",
          "type": "integer",
          "description": "超时时间（秒）",
          "required": false
        }
      ],
      "handler": "python3 ./main.py"
    }
  ]
}